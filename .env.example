# V2Ray Management System Environment Configuration
# پیکربندی محیطی سیستم مدیریت V2Ray

SUDO_USER=root
# =================================================================
# Domain Configuration | پیکربندی دامنه
# =================================================================
# Your domain name (Required) | نام دامنه شما (اجباری)
# Example: example.com
DOMAIN=panel.rookari.com

# =================================================================
# Telegram Bot Configuration | پیکربندی ربات تلگرام
# =================================================================
# Telegram Bot Token (Required) | توکن ربات تلگرام (اجباری)
# Get it from @BotFather | از @BotFather دریافت کنید
# Example: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
BOT_TOKEN=6505866524:AAHqmVU4gviUFiCFHRSoo-Zsy_uM6mGUA38

# Telegram Admin ID (Required) | شناسه تلگرام مدیر (اجباری)
# Get it from @userinfobot | از @userinfobot دریافت کنید
# Example: 123456789
ADMIN_ID=1713374557

# =================================================================
# Database Configuration | پیکربندی پایگاه داده
# =================================================================
# Database Password (Required) | رمز عبور پایگاه داده (اجباری)
# Min 8 characters | حداقل ۸ کاراکتر
# Example: MyStr0ngP@ssw0rd
DB_PASSWORD=your-db-password

# Database Host (Optional) | آدرس پایگاه داده (اختیاری)
# Default: localhost | پیش‌فرض: localhost
DB_HOST=localhost

# Database Port (Optional) | پورت پایگاه داده (اختیاری)
# Default: 5432 | پیش‌فرض: 5432
DB_PORT=5432

# Database Name (Optional) | نام پایگاه داده (اختیاری)
# Default: v2ray | پیش‌فرض: v2ray
DB_NAME=v2ray

# Database User (Optional) | نام کاربری پایگاه داده (اختیاری)
# Default: postgres | پیش‌فرض: postgres
DB_USER=postgres

# =================================================================
# Security Configuration | پیکربندی امنیتی
# =================================================================
# JWT Secret Key (Required) | کلید رمزنگاری JWT (اجباری)
# Min 32 characters | حداقل ۳۲ کاراکتر
# Example: my-super-secret-key-that-is-very-long-and-secure
JWT_SECRET=my-super-secret-key-that-is-very-long-and-secure

# SSL Email (Required) | ایمیل SSL (اجباری)
# Used for Let's Encrypt notifications | برای اطلاع‌رسانی‌های Let's Encrypt استفاده می‌شود
# Example: admin@example.com
SSL_EMAIL=your-email@domain.com

# =================================================================
# Application Configuration | پیکربندی برنامه
# =================================================================
# Node Environment (Optional) | محیط اجرای نود (اختیاری)
# Values: development, production | مقادیر: development, production
# Default: production | پیش‌فرض: production
NODE_ENV=production

# API Port (Optional) | پورت API (اختیاری)
# Default: 3000 | پیش‌فرض: 3000
API_PORT=3000

# Web Port (Optional) | پورت وب (اختیاری)
# Default: 3001 | پیش‌فرض: 3001
WEB_PORT=3001

# =================================================================
# V2Ray Configuration | پیکربندی V2Ray
# =================================================================
# V2Ray Port (Optional) | پورت V2Ray (اختیاری)
# Default: 8388 | پیش‌فرض: 8388
V2RAY_PORT=8388

# V2Ray Protocol (Optional) | پروتکل V2Ray (اختیاری)
# Values: vmess, vless | مقادیر: vmess, vless
# Default: vmess | پیش‌فرض: vmess
V2RAY_PROTOCOL=vmess

# =================================================================
# Backup Configuration | پیکربندی پشتیبان‌گیری
# =================================================================
# Backup Schedule (Optional) | زمان‌بندی پشتیبان‌گیری (اختیاری)
# Cron format | فرمت Cron
# Default: 0 0 * * * (daily at midnight) | پیش‌فرض: 0 0 * * * (روزانه در نیمه‌شب)
BACKUP_SCHEDULE=0 0 * * *

# Backup Retention Days (Optional) | تعداد روزهای نگهداری پشتیبان (اختیاری)
# Default: 7 | پیش‌فرض: 7
BACKUP_RETENTION_DAYS=7

# =================================================================
# Installation Information | اطلاعات نصب
# =================================================================
# Installation Date (Auto-generated) | تاریخ نصب (خودکار تنظیم می‌شود)
# Format: YYYY-MM-DD HH:mm:ss | فرمت: YYYY-MM-DD HH:mm:ss
INSTALL_DATE=

# Installation Version (Auto-generated) | نسخه نصب (خودکار تنظیم می‌شود)
INSTALL_VERSION=

